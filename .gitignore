# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.zip
/apphub-app-creator
.DS_Store

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
# binaries
dist/


# ignore changes to launch.json
.vscode/launch.json

#json files
*.json

!samples/*/*.zip
!samples/*.json
!samples/*/*.json
!test/*.json
!test/*.yaml
!test/*.zip
!cmd/apphub-app-creator*
!internal/apphub-app-creator*
!frontend/package.json
!frontend/package-lock.json
!.vscode/launch.json
!.devcontainer/devcontainer.json

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vscode/

# Web UI assets embedded into the binary at container-build time (see
# internal/cmd/webui_embed.go); generated from `npm run build`, never committed.
internal/cmd/webdist/

# Agent skills
.agents/skills/

